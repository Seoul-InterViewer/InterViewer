import { ModalType } from "./modal.type";
import { modalVariants } from "./modal.variants";

export const Modal = ({
  isOpen,
  onClose,
  title,
  children,
  buttons,
  showCloseButton,
  className,
}: ModalType) => {
    return (
        <div></div>
    )
};
